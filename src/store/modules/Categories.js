const name = "CATEGORIES";
const namespaced = true;
const state = {
  all: []
};
const getters = {
  STATE: (s) => s,
};
const mutations = {
};
const actions = {
  GET_ALL_CATEGORIES: async ({ commit }) => {

  }
};
export default {
  name,
  namespaced,
  state,
  getters,
  mutations,
  actions,
};
